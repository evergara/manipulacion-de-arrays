const letters = ['a', 'b', 'C', 'd'];
const animales

for (let index = 0; index < letters.length; index++) {
    const element = Letters[index];
    console.log('FOR', letters.toUpperCase());
}

letters.forEach(item => {
    console.log('forEach',item.toUpperCase());
});

funtion()